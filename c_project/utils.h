// Ensure that input values meet specific constraints

#ifndef UTILS_H
#define UTILS_H

int inputPositiveInt();  // Reads and validates a positive integer
float inputGPA();    // Reads and validates a GPA between 0.0 and 4.0

#endif
